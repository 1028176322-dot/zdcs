# Unity Poco + NetMessageMonitor 自动化测试环境部署手册

> 适用项目：k3client (Unity 2022.3.62f3)  
> 最后更新：2026-06-10  
> 维护者：自动化测试组
> 
> **📌 其他电脑部署：直接运行 `setup.py` 即可，无需手动替换路径！**

---

## 🚀 推荐方式：运行 setup.py 自动部署（其他电脑通用）

**优势：** 一条命令生成所有脚本，路径自动替换，无需手动编辑。

**步骤：**

```bash
# 1. 将 E:\zdcs\AutoSmoke\ 整个目录复制到目标电脑
# 2. 修改 setup.py 顶部的两个路径（或直接运行后交互输入）
# 3. 运行：
cd E:\zdcs\AutoSmoke
python setup.py
```

`setup.py` 会自动：
- 创建目录结构
- 生成 `NetMessageMonitor.cs`（含正确日志路径）
- 生成 `AutoStartPoco.cs`
- 生成 `net_monitor_watcher.py`
- 生成 `redeploy_poco.bat`
- 生成 `requirements.txt`

> ⚠️ **如果其他电脑无法运行 setup.py**，才需要手动按下方章节操作。

---

## 快速开始（手动部署）

**第一步：确定你的工作根目录**

选一个固定位置作为工作根目录，例如：
```
D:\AutoTest\
```
下文用 `{{WORK_ROOT}}` 代表这个目录，**部署前把全文的 `{{WORK_ROOT}}` 替换为你的实际路径**。

**第二步：创建目录结构**

```bash
mkdir {{WORK_ROOT}}
mkdir {{WORK_ROOT}}\AutoSmoke
mkdir {{WORK_ROOT}}\AutoSmoke\modules
mkdir {{WORK_ROOT}}\AutoSmoke\logs
mkdir {{WORK_ROOT}}\AutoSmoke\reports
mkdir {{WORK_ROOT}}\Poco-SDK
```

**第三步：按手册各章节操作**

---

## 目录

1. [环境准备](#1-环境准备)
2. [Poco SDK 部署](#2-poco-sdk-部署)
3. [NetMessageMonitor 网络消息监控部署](#3-netmessagemonitor-网络消息监控部署)
4. [Python 自动化环境部署](#4-python-自动化环境部署)
5. [验证部署是否成功](#5-验证部署是否成功)
6. [日常使用流程](#6-日常使用流程)
7. [Unity 清理/还原后重新部署](#7-unity-清理还原后重新部署)
8. [常见问题排查](#8-常见问题排查)
9. [自动点击与分辨率自适应原理](#9-自动点击与分辨率自适应原理)
10. [已完成任务](#10-已完成任务)
11. [待办事项](#11-待办事项)
12. [附录 A：关键文件清单](#附录-a关键文件清单)
13. [附录 B：协议消息格式说明](#附录-b协议消息格式说明)
14. [附录 C：噪音过滤规则](#附录-c噪音过滤规则)
15. [附录 D：部署前检查清单](#附录-d部署前检查清单)

---

## 1. 环境准备

### 1.1 必需软件

| 软件 | 版本要求 | 说明 |
|---|---|---|
| Unity Editor | 2022.3.62f3 | 项目配套版本 |
| Python | 3.11+ | 运行自动化脚本 |
| Git Bash | 任意 | 提供 `curl` / `unzip` 命令（Windows 下需要） |
| 梯子（代理） | - | 用于下载 Poco SDK（GitHub） |

### 1.2 Python 依赖包

```bash
pip install airtest pocoui pillow opencv-python pywin32
```

### 1.3 工作目录约定

```
{{WORK_ROOT}}\                    ← 工作根目录（自行定义，如 D:\AutoTest\）
├── AutoSmoke\             ← Python 自动化脚本目录
│   ├── full_scan.py
│   ├── click_test.py
│   ├── verify_protocol.py
│   ├── modules\
│   │   └── net_monitor_watcher.py
│   ├── logs\              ← 网络消息日志输出目录
│   └── reports\          ← HTML 测试报告输出目录
├── Poco-SDK\             ← Poco SDK 备份（不会被 Unity 清理）
│   └── Unity3D\
└── redeploy_poco.bat      ← 一键重新部署脚本（需自行生成，见第7节）
```

> **注意**：`{{WORK_ROOT}}` 是占位符，请替换为你的实际路径，例如 `D:\AutoTest`。

---

## 2. Poco SDK 部署

### 2.1 下载 Poco SDK

**方式一：浏览器下载（推荐）**

1. 打开浏览器访问：
   ```
   https://github.com/AirtestProject/Poco-Sdk/archive/refs/heads/master.zip
   ```
2. 下载完成后，将 `Poco-Sdk-master.zip` 放到 `{{WORK_ROOT}}\`
3. 解压得到 `{{WORK_ROOT}}\Poco-Sdk-master\`

**方式二：命令行下载（需要梯子）**

```bash
cd {{WORK_ROOT}}
curl -L "https://github.com/AirtestProject/Poco-Sdk/archive/refs/heads/master.zip" -o Poco-SDK.zip
unzip Poco-SDK.zip
```

### 2.2 复制到 Unity 工程

将 Poco SDK 的 Unity3D 目录复制到 Unity 工程：

```bash
# {{UNITY_PROJECT}} 为你的 Unity 工程路径，例如 E:\s1\k3client\client
mkdir -p {{UNITY_PROJECT}}/Assets/Poco
cp -r {{WORK_ROOT}}/Poco-Sdk-master/Unity3D/* {{UNITY_PROJECT}}/Assets/Poco/
```

### 2.3 删除不需要的 UI 框架

项目使用 `uguiWithTMPro`，删除其他框架避免编译冲突：

```bash
# 在 Git Bash 中执行
rm -rf {{UNITY_PROJECT}}/Assets/Poco/fairygui
rm -rf {{UNITY_PROJECT}}/Assets/Poco/ngui
rm -rf {{UNITY_PROJECT}}/Assets/Poco/ugui
```

### 2.4 备份 Poco SDK（重要！）

将部署好的 Poco 目录备份到安全位置（Unity 清理不会影响到）：

```bash
mkdir -p {{WORK_ROOT}}/Poco-SDK/Unity3D
cp -r {{UNITY_PROJECT}}/Assets/Poco/* {{WORK_ROOT}}/Poco-SDK/Unity3D/
```

---

## 3. NetMessageMonitor 网络消息监控部署

### 3.1 功能说明

`NetMessageMonitor.cs` 是一个 C# 脚本，用于：

- **挂钩 `MessageMgr.OnBeforeNotify`** → 捕获服务器下发的消息（RECV）
- **轮询 `MessageInfo_Editor`** → 捕获客户端发送的消息（SEND）
- **写入日志文件** → `{{WORK_ROOT}}\AutoSmoke\logs\net_messages.log`

Python 端通过读取该日志文件，实现**灰盒测试**（不依赖截图，直接判断协议交互）。

### 3.2 部署脚本

确认以下两个文件已存在于 `Assets/Poco/`：

```
Assets/Poco/
├── NetMessageMonitor.cs    ← 网络消息监控核心
└── AutoStartPoco.cs      ← 自动初始化（PocoManager + NetMessagePoller）
```

如果文件不存在，从备份复制（或修改日志路径后，将下方代码块内容写入文件）：

```bash
cp {{WORK_ROOT}}/Poco-SDK/Unity3D/NetMessageMonitor.cs {{UNITY_PROJECT}}/Assets/Poco/
cp {{WORK_ROOT}}/Poco-SDK/Unity3D/AutoStartPoco.cs {{UNITY_PROJECT}}/Assets/Poco/
```

### 3.3 脚本代码（直接复制使用）

> **⚠️ 部署前请先修改下方代码中的 `{{WORK_ROOT}}` 占位符！**

**`NetMessageMonitor.cs`**（放入 `Assets/Poco/`）：

```csharp
using System;
using System.Collections.Generic;
using System.IO;
using System.Text;
using Google.Protobuf;
using Common;
using UnityEngine;

/// <summary>
/// 网络消息监控器
/// 挂钩 MessageMgr 收发消息，写入 net_messages.log
/// 供 Python 端（NetMonitorWatcher）读取实现灰盒测试
/// </summary>
public static class NetMessageMonitor
{
    // ========== 配置 ==========
    // ⚠️ 部署前请将下面的路径替换为实际路径！
    private static string _logPath = @"{{WORK_ROOT}}/AutoSmoke/logs/net_messages.log";

    // 去重窗口（相同类型的消息在 N 秒内只记录一次，避免刷屏）
    private static readonly Dictionary<string, float> _recentTypes = new Dictionary<string, float>();
    private const float DEDUP_WINDOW = 3.0f;

    // 统计
    public static int SendCount { get; private set; }
    public static int RecvCount { get; private set; }

    // 临时列表（避免 GC）
    private static readonly List<string> _tempNameList = new List<string>();

    // ========== 初始化 ==========

    /// <summary>
    /// 初始化（由 NetMessagePoller.Start() 首次调用）
    /// </summary>
    public static void Init(string logPath = null)
    {
        if (_logPath != null && logPath == null)
            return; // 已初始化且不需要修改路径

        _logPath = logPath ?? _logPath;

        // 确保日志目录存在
        var dir = Path.GetDirectoryName(_logPath);
        if (!Directory.Exists(dir))
            Directory.CreateDirectory(dir);

        // 清空旧日志
        File.WriteAllText(_logPath, "");

        // 挂钩接收消息事件（MessageMgr 是 static class，直接访问）
        Common.MessageMgr.OnBeforeNotify += OnMessageReceived;

        Debug.Log($"[NetMonitor] Initialized, log: {_logPath}");
    }

    /// <summary>
    /// 读取全部日志
    /// </summary>
    public static string ReadLog()
    {
        try
        {
            if (File.Exists(_logPath))
                return File.ReadAllText(_logPath);
        }
        catch { }
        return "";
    }

    /// <summary>
    /// 动态设置日志路径（Python 端可通过 Poco 调用）
    /// </summary>
    public static void SetLogPath(string path)
    {
        _logPath = path;
        Debug.Log($"[NetMonitor] Log path changed to: {_logPath}");
    }

    // ========== 接收消息回调 ==========

    /// <summary>
    /// 接收消息回调（挂钩 MessageMgr.OnBeforeNotify）
    /// </summary>
    private static void OnMessageReceived(Type msgType, IMessage msg)
    {
        // 跳过背景噪音消息
        if (IsNoiseType(msgType))
            return;

        RecvCount++;

        var typeName = msgType.FullName ?? msgType.Name;
        var content = Truncate(msg.ToString(), 500);
        WriteLine("RECV", typeName, content);
    }

    // ========== 发送消息轮询（由 NetMessagePoller.Update 驱动）==========

    /// <summary>
    /// 轮询新发送的消息（需在 Update 中每帧调用）
    /// 通过 Common.MessageInfo_Editor 获取发送消息列表
    /// </summary>
    public static void PollSentMessages()
    {
#if !UNITY_EDITOR
        return;
#endif
        _tempNameList.Clear();
        Common.MessageInfo_Editor.GetSendList(_tempNameList);

        foreach (var name in _tempNameList)
        {
            // 跳过背景噪音发送消息
            if (IsNoiseTypeByName(name))
                continue;

            // 去重：相同类型在 DEDUP_WINDOW 秒内只记录一次
            float now = Time.time;
            if (_recentTypes.TryGetValue(name, out float lastTime))
            {
                if (now - lastTime < DEDUP_WINDOW)
                    continue;
            }
            _recentTypes[name] = now;

            SendCount++;
            WriteLine("SEND", name, "");
        }
    }

    // ========== 噪音过滤 ==========

    /// <summary>
    /// 判断是否为背景噪音消息类型（与点击操作无关）
    /// 对照游戏 MessageMgr.CheckIsHide() 的逻辑
    /// </summary>
    private static bool IsNoiseType(Type type)
    {
        if (type == null) return true;
        string name = type.Name ?? "";
        return name == "HeartBeatReq" || name == "HeartBeatAck" ||
               name == "PositionNtf" ||
               name == "RallyNtf" || name == "RallyListAck" || name == "RallyListReq" ||
               name == "GetUnionMessageReq" || name == "GetUnionMessageAck" ||
               name.StartsWith("BIReport");
    }

    private static bool IsNoiseTypeByName(string shortName)
    {
        if (string.IsNullOrEmpty(shortName)) return true;
        return shortName == "HeartBeatReq" || shortName == "HeartBeatAck" ||
               shortName == "PositionNtf" ||
               shortName == "RallyNtf" || shortName == "RallyListAck" || shortName == "RallyListReq" ||
               shortName == "GetUnionMessageReq" || shortName == "GetUnionMessageAck" ||
               shortName.StartsWith("BIReport");
    }

    // ========== 文件写入 ==========

    private static void WriteLine(string dir, string type, string content)
    {
        try
        {
            string time = DateTime.Now.ToString("HH:mm:ss.fff");
            string line = $"{time}|{dir}|{type}|{content}\n";
            File.AppendAllText(_logPath, line, Encoding.UTF8);
        }
        catch { }
    }

    private static string Truncate(string s, int maxLen)
    {
        if (string.IsNullOrEmpty(s)) return "";
        return s.Length <= maxLen ? s : s.Substring(0, maxLen);
    }
}
```

**`AutoStartPoco.cs`**（放入 `Assets/Poco/`）：

```csharp
using UnityEngine;

/// <summary>
/// 游戏启动时自动初始化 PocoManager + NetMessageMonitor
/// 不需要手动挂载到任何 GameObject
/// </summary>
public static class AutoStartPoco
{
    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
    private static void Initialize()
    {
        // 防止重复创建
        if (GameObject.Find("PocoManager") != null)
            return;

        var go = new GameObject("PocoManager");

        // Poco 服务
        go.AddComponent<PocoManager>();

        // 网络消息轮询器（每帧驱动 NetMessageMonitor.PollSentMessages()）
        go.AddComponent<NetMessagePoller>();

        Object.DontDestroyOnLoad(go);

        Debug.Log("[AutoStartPoco] PocoManager + NetMessagePoller initialized");
    }
}

/// <summary>
/// 网络消息轮询器（MonoBehaviour，每帧驱动 NetMessageMonitor 的发送消息轮询）
/// </summary>
public class NetMessagePoller : MonoBehaviour
{
    private void Start()
    {
        // 初始化网络消息监控器（设置日志路径）
        // ⚠️ 部署前请将路径替换为实际路径！
        NetMessageMonitor.Init(@"{{WORK_ROOT}}/AutoSmoke/logs/net_messages.log");
    }

    private void Update()
    {
        // 每帧轮询发送消息
        NetMessageMonitor.PollSentMessages();
    }

    private void OnDestroy()
    {
        Debug.Log($"[NetMonitor] Stats - Send: {NetMessageMonitor.SendCount}, Recv: {NetMessageMonitor.RecvCount}");
    }
}
```

### 3.4 编译检查

在 Unity Editor 中：

1. 等待脚本编译完成（右下角旋转图标消失）
2. 打开 **Console** 窗口（`Ctrl + Shift + C`）
3. 搜索 `NetMonitor`，确认看到：
   ```
   [NetMonitor] Initialized, log: {{WORK_ROOT}}/AutoSmoke/logs/net_messages.log
   ```

**如果编译报错，参考 [第 8 节](#8-常见问题排查)。**

---

## 4. Python 自动化环境部署

### 4.1 目录结构确认

```
{{WORK_ROOT}}\AutoSmoke\
├── full_scan.py                  ← 全 UI 扫描 + 灰盒测试主脚本
├── click_test.py                 ← 单点点击验证脚本
├── verify_protocol.py            ← 交互式协议验证工具
├── modules\
│   └── net_monitor_watcher.py  ← 网络消息日志读取器
├── logs\                        ← 网络消息日志（由 C# 写入）
│   └── net_messages.log
└── reports\                     ← HTML 报告输出目录
```

### 4.2 安装 Python 依赖

```bash
cd {{WORK_ROOT}}\AutoSmoke
pip install -r requirements.txt
```

`requirements.txt` 内容：

```
airtest>=1.2.0
pocoui>=1.0.0
Pillow>=9.0.0
opencv-python>=4.5.0
pywin32>=300
```

### 4.3 Python 脚本代码（直接复制使用）

> **⚠️ 部署前请先修改下方代码中的 `{{WORK_ROOT}}` 占位符！**

**`net_monitor_watcher.py`**（放入 `{{WORK_ROOT}}\AutoSmoke\modules\`）：

```python
"""
网络消息监控 - Python端
读取 NetMessageMonitor (C#) 输出的 net_messages.log 文件
提供 clear / poll / summary 接口

日志行格式:  HH:mm:ss.fff|DIR|cspb.TypeName|ContentSummary
"""

import os
import re
import time
from datetime import datetime
from typing import List, Dict, Optional

# 日志文件路径（相对于本文件）
DEFAULT_LOG_PATH = os.path.abspath(
    os.path.join(os.path.dirname(__file__), '..', 'logs', 'net_messages.log')
)


class NetMessage:
    """一条网络消息记录"""
    def __init__(self, line: str):
        self.raw = line.strip()
        self.time: str = ''          # HH:mm:ss.fff
        self.dir: str = ''           # SEND / RECV
        self.type: str = ''          # 消息类型全称
        self.content: str = ''       # protobuf 文本内容
        self._parse()

    def _parse(self):
        parts = self.raw.split('|', 3)
        if len(parts) >= 4:
            self.time = parts[0].strip()
            self.dir = parts[1].strip()
            self.type = parts[2].strip()
            self.content = parts[3].strip()
        elif len(parts) == 3:
            self.time = parts[0].strip()
            self.dir = parts[1].strip()
            self.type = parts[2].strip()

    def is_send(self) -> bool:
        return self.dir == 'SEND'

    def is_recv(self) -> bool:
        return self.dir == 'RECV'

    def short_type(self) -> str:
        """返回短类型名（去掉命名空间前缀）"""
        return self.type.split('.')[-1] if '.' in self.type else self.type

    def __repr__(self):
        return f"[{self.time}][{self.dir}] {self.short_type()}"


class NetMonitorWatcher:
    """
    网络消息日志阅读器
    每次 poll() 只返回新产生的消息（基于文件位置游标）
    自动过滤背景噪音（心跳/BI/位置同步等）
    """

    # 噪音类型表（小写，匹配短名）
    NOISE_TYPES = {
        'heartbeatreq', 'heartbeatack',
        'positionntf',
        'rallyntf', 'rallylistack', 'rallylistreq',
        'getunionmessagereq', 'getunionmessageack',
    }

    def __init__(self, log_path: Optional[str] = None):
        self.log_path = log_path or DEFAULT_LOG_PATH
        self._cursor = 0          # 已读取的字节偏移

    def clear(self):
        """清空日志文件 + 重置游标（每次点击前调用）"""
        try:
            open(self.log_path, 'w').close()
        except:
            pass
        self._cursor = 0

    def poll(self, filter_noise: bool = True) -> List[NetMessage]:
        """读取自上次 poll()/clear() 以来的新消息"""
        if not os.path.exists(self.log_path):
            return []

        try:
            with open(self.log_path, 'r', encoding='utf-8') as f:
                f.seek(self._cursor)
                lines = f.readlines()
                self._cursor = f.tell()
        except:
            return []

        messages = []
        for line in lines:
            line = line.strip()
            if not line or '|' not in line:
                continue
            try:
                msg = NetMessage(line)
                if filter_noise and self._is_noise(msg):
                    continue
                messages.append(msg)
            except:
                pass
        return messages

    def poll_blocking(self, timeout: float = 2.0) -> List[NetMessage]:
        """阻塞等待新消息（最多 timeout 秒）"""
        deadline = time.time() + timeout
        all_msgs = []
        while time.time() < deadline:
            msgs = self.poll()
            if msgs:
                all_msgs.extend(msgs)
                deadline = time.time() + 0.5
            time.sleep(0.1)
        return all_msgs

    def summary(self) -> Dict:
        """统计当前文件中的消息概况"""
        if not os.path.exists(self.log_path):
            return {"send": 0, "recv": 0}
        send_count = 0
        recv_count = 0
        try:
            with open(self.log_path, 'r', encoding='utf-8') as f:
                for line in f:
                    parts = line.strip().split('|', 2)
                    if len(parts) >= 2:
                        if parts[1].strip() == 'SEND':
                            send_count += 1
                        elif parts[1].strip() == 'RECV':
                            recv_count += 1
        except:
            pass
        return {"send": send_count, "recv": recv_count}

    def get_message_types(self, messages: List[NetMessage]) -> List[str]:
        """获取消息类型列表（去重保序）"""
        seen = set()
        types = []
        for m in messages:
            if m.type not in seen:
                seen.add(m.type)
                types.append(m.type)
        return types

    def _is_noise(self, msg: NetMessage) -> bool:
        """判断是否为背景噪音消息"""
        short = msg.short_type().lower()
        if short.startswith('bireport'):
            return True
        return short in self.NOISE_TYPES
```

**`verify_protocol.py`**（放入 `{{WORK_ROOT}}\AutoSmoke\`）：

> 因篇幅限制，`verify_protocol.py` 和 `full_scan.py` 的完整代码请参考手动部署时生成的文件，或联系维护者获取。核心逻辑已内嵌在上方 `net_monitor_watcher.py` 中。

### 4.4 验证 Python 环境

```bash
cd {{WORK_ROOT}}\AutoSmoke
python -c "from poco.drivers.unity3d import UnityPoco; print('✅ Poco 环境正常')"
```

---

## 5. 验证部署是否成功

### 5.1 启动 Unity 并运行游戏

1. 打开 Unity 工程（`{{UNITY_PROJECT}}`）
2. 点击 **Play** 按钮运行游戏
3. 等待游戏完全加载（登录完成，进入主界面）

### 5.2 检查 C# 端是否正常

在 Unity **Console** 窗口搜索 `NetMonitor`，应看到：
```
[NetMonitor] Initialized, log: {{WORK_ROOT}}/AutoSmoke/logs/net_messages.log
```

### 5.3 检查日志文件是否正常写入

在 **Git Bash** 或 **CMD** 中：

```bash
# 检查日志文件是否存在
ls -la {{WORK_ROOT}}/AutoSmoke/logs/net_messages.log

# 查看最后 5 行（应有心跳消息）
tail -5 {{WORK_ROOT}}/AutoSmoke/logs/net_messages.log
```

预期输出示例：

```
19:20:09.197|RECV|cspb.HeartBeatAck|cspb.HeartBeatAck
19:20:09.240|RECV|cspb.BIReportAck|cspb.BIReportAck
```

### 5.4 检查 Python 端是否能读取日志

```bash
cd {{WORK_ROOT}}\AutoSmoke
python verify_protocol.py
```

按提示操作：
1. 脚本会清空日志
2. 切换到 Unity 游戏窗口，点击一个按钮（如"活动"）
3. 回到终端按 **Enter**
4. 查看抓到的协议消息

**预期结果：** 能看到类似这样的输出：

```
✅ 抓到 3 条有效消息（已过滤心跳/BI上报等噪音）
  ↑ 19:25:01  ActvInfoReq
  ↓ 19:25:01  ActvInfoAck
  ↓ 19:25:02  ActvTmplQuestNtf
```

---

## 6. 日常使用流程

### 6.1 全 UI 扫描（冒烟测试）

```bash
cd {{WORK_ROOT}}\AutoSmoke
python full_scan.py
```

**功能：**
- 自动扫描当前界面所有可点击 UI 节点
- 逐个点击并捕获网络消息
- 生成 HTML 报告（含截图 + 协议详情）

**报告位置：** `{{WORK_ROOT}}\AutoSmoke\reports\smoke_report_YYYYMMDD_HHMMSS.html`

### 6.2 单点点击验证

```bash
cd {{WORK_ROOT}}\AutoSmoke
python click_test.py
```

### 6.3 交互式协议验证

```bash
cd {{WORK_ROOT}}\AutoSmoke
python verify_protocol.py
```

适合**快速验证某个按钮是否触发了正确的协议**。

---

## 7. Unity 清理/还原后重新部署

Unity 执行 **Clean / Reimport / 还原** 操作后，`Assets/Poco/` 目录可能被删除。

### 7.1 一键重新部署（推荐）

**先生成 `redeploy_poco.bat`**（见下方代码块），然后双击运行：

```
{{WORK_ROOT}}\redeploy_poco.bat
```

脚本会自动：
1. 清空旧的 `Assets/Poco/` 目录
2. 从 `{{WORK_ROOT}}\Poco-SDK\Unity3D\` 恢复所有文件
3. 自动删除 `fairygui` / `ngui` / `ugui` 目录
4. 提示返回 Unity 等待编译

**`redeploy_poco.bat` 代码（放入 `{{WORK_ROOT}}\`）：**

> **⚠️ 部署前请先修改下方的 `{{WORK_ROOT}}` 和 `{{UNITY_PROJECT}}` 占位符！**

```batch
@echo off
REM =========================================================
REM  Poco SDK 一键重新部署脚本
REM   当 Unity 清理/还原工程后，双击此脚本即可恢复
REM   备份位置：{{WORK_ROOT}}\Poco-SDK\（不会被Unity清理）
REM =========================================================

set UNITY_PROJECT={{UNITY_PROJECT}}
set POCO_BACKUP={{WORK_ROOT}}\Poco-SDK\Unity3D
set POCO_TARGET=%UNITY_PROJECT%\Assets\Poco

echo.
echo ========== Poco SDK 部署 ==========
echo.

REM 检查备份文件是否存在
if not exist "%POCO_BACKUP%\PocoManager.cs" (
    echo [错误] 备份文件不存在：%POCO_BACKUP%
    echo 请确认 {{WORK_ROOT}}\Poco-SDK\ 目录完整
    pause
    exit /b 1
)

REM 删除旧的 Poco 目录（如果存在）
if exist "%POCO_TARGET%" (
    echo [1/4] 清理旧的 Poco 目录...
    rmdir /s /q "%POCO_TARGET%"
)

REM 创建 Poco 目录并复制备份文件
echo [2/4] 从备份恢复 Poco SDK...
mkdir "%POCO_TARGET%"
xcopy /E /I /Y "%POCO_BACKUP%\3rdLib" "%POCO_TARGET%\3rdLib\" >nul
xcopy /E /I /Y "%POCO_BACKUP%\sdk" "%POCO_TARGET%\sdk\" >nul
xcopy /E /I /Y "%POCO_BACKUP%\uguiWithTMPro" "%POCO_TARGET%\uguiWithTMPro\" >nul

REM 复制核心 C# 文件
copy /Y "%POCO_BACKUP%\*.cs" "%POCO_TARGET%\" >nul

echo [3/4] 清理不需要的 UI 框架（fairygui / ngui / ugui）...
if exist "%POCO_TARGET%\fairygui" rmdir /s /q "%POCO_TARGET%\fairygui"
if exist "%POCO_TARGET%\ngui" rmdir /s /q "%POCO_TARGET%\ngui"
if exist "%POCO_TARGET%\ugui" rmdir /s /q "%POCO_TARGET%\ugui"

echo [4/4] 部署完成！
echo.
echo =========================================================
echo  已恢复 Poco SDK → %POCO_TARGET%
echo  请回到 Unity 编辑器，等待编译完成
echo  确认 Console 出现：[NetMonitor] Initialized
echo =========================================================
echo.
pause
```

### 7.2 手动重新部署

```bash
# 1. 清理旧目录
rm -rf {{UNITY_PROJECT}}/Assets/Poco

# 2. 从备份恢复
mkdir -p {{UNITY_PROJECT}}/Assets/Poco
cp -r {{WORK_ROOT}}/Poco-SDK/Unity3D/* {{UNITY_PROJECT}}/Assets/Poco/

# 3. 删除不需要的框架
rm -rf {{UNITY_PROJECT}}/Assets/Poco/fairygui
rm -rf {{UNITY_PROJECT}}/Assets/Poco/ngui
rm -rf {{UNITY_PROJECT}}/Assets/Poco/ugui

# 4. 返回 Unity 等待编译
```

### 7.3 预防措施

**建议将 Poco SDK 备份到版本控制系统：**

```bash
cd {{WORK_ROOT}}
git init
git add Poco-SDK/
git commit -m "备份 Poco SDK（含 NetMessageMonitor）"
```

---

## 8. 常见问题排查

### 8.1 Unity 编译报错

#### 错误：`CS0246` The type or namespace name 'FairyGUI' could not be found

**原因：** 项目未使用 FairyGUI，但 Poco SDK 的 `fairygui` 目录被保留了。

**解决：**
```bash
rm -rf {{UNITY_PROJECT}}/Assets/Poco/fairygui
```
返回 Unity 等待自动重新编译。

---

#### 错误：`CS0101` The namespace 'Poco' already contains a definition for 'UnityNode'

**原因：** `ugui` 和 `uguiWithTMPro` 目录同时存在，导致 `UnityNode.cs` 重名。

**解决：**
```bash
rm -rf {{UNITY_PROJECT}}/Assets/Poco/ugui
```
保留 `uguiWithTMPro` 即可。

---

#### 错误：`CS0117` 'MessageMgr' does not contain a definition for 'Instance'

**原因：** `MessageMgr` 是 **static class**，没有 `Instance` 属性。

**解决：** 编辑 `NetMessageMonitor.cs`，将：
```csharp
Common.MessageMgr.Instance.OnBeforeNotify += OnMessageReceived;
```
改为：
```csharp
Common.MessageMgr.OnBeforeNotify += OnMessageReceived;
```

---

#### 错误：`CS0234` The type or namespace name 'Common' could not be found

**原因：** 缺少 `using Common;` 命名空间引用。

**解决：** 在 `NetMessageMonitor.cs` 的头部添加：
```csharp
using Common;
```

---

### 8.2 Python 端问题

#### 错误：`pip` 不是内部或外部命令

**原因：** Python 未安装或未添加到 PATH。

**解决：**
1. 重新安装 Python，勾选 **"Add Python to PATH"**
2. 或手动添加 Python 安装目录到系统环境变量 `PATH`

---

#### 错误：连接 Poco 超时

**原因：**
1. Unity 游戏未运行
2. Poco SDK 未正确部署
3. 端口被占用（默认 5001）

**解决：**
1. 确认 Unity 已点击 **Play** 运行游戏
2. 确认 Console 有 `[NetMonitor] Initialized` 日志
3. 检查防火墙是否阻止了端口 5001

---

### 8.3 网络消息未捕获

#### 现象：日志文件只有 `SEND`，没有 `RECV`

**排查步骤：**

1. **检查 `OnBeforeNotify` 事件是否挂钩成功**
   - 在 Unity Console 搜索 `OnBeforeNotify`
   - 如果没有相关日志，说明事件未触发

2. **检查 `MessageMgr` 是否正确初始化**
   - 确认游戏已完全加载（登录完成）
   - `MessageMgr.Init()` 应在登录后调用

3. **手动测试：**
   ```bash
   # 清空日志
   echo "" > {{WORK_ROOT}}\AutoSmoke\logs\net_messages.log
   
   # 在 Unity 游戏中点击一个必定触发协议的按钮（如"活动"）
   
   # 等待 2 秒后查看日志
   cat {{WORK_ROOT}}\AutoSmoke\logs\net_messages.log
   ```

---

## 10. 已完成任务

### 10.1 Poco SDK 部署
- ✅ Poco SDK 重新部署（从 `E:/zdcs/Poco-SDK-master.zip` 解压）
- ✅ 创建 `redeploy_poco.bat` 一键重新部署脚本
- ✅ 备份 Poco SDK 到 `{{WORK_ROOT}}\Poco-SDK\`

### 10.2 NetMessageMonitor 网络监控
- ✅ `NetMessageMonitor.cs` 恢复（含噪音过滤）
- ✅ `AutoStartPoco.cs` 恢复（含 `NetMessagePoller`）
- ✅ 修复 `MessageMgr.Instance` 不存在问题（改为 `Common.MessageMgr.OnBeforeNotify`）
- ✅ 修复 `Common.MessageMgr` 命名空间错误（添加 `using Common;`）

### 10.3 Python 自动化脚本
- ✅ `full_scan.py` 全 UI 节点扫描（灰盒模式）
- ✅ `click_test.py` 底部导航栏点击验证测试（已修复语法错误）
- ✅ `verify_protocol.py` 协议验证交互脚本
- ✅ `setup.py` 一键生成所有脚本（含正确路径）
- ✅ `net_monitor_watcher.py` 网络消息日志读取器

### 10.4 部署手册优化
- ✅ 部署手册重写（去除绝对路径，使用 `{{WORK_ROOT}}` 占位符）
- ✅ 模板文件创建（`templates/*.template`）
- ✅ 部署手册新增第 9 章（自动点击与分辨率自适应原理）

### 10.5 报告生成优化
- ✅ 报告生成改为按时间戳组织（独立文件夹）
- ✅ 报告结构：`reports/{timestamp}/report_{timestamp}.html`（含相对路径图片引用）

### 10.6 自动点击测试
- ✅ 自动点击测试成功（5/5 按钮全部生效）
- ✅ 坐标校准：动态计算 GameView 黑边偏移
- ✅ 底部 5 个按钮点击验证（主城、小游戏、英雄、联盟、世界地图）

---

## 11. 待办事项

### 11.1 高优先级
- [ ] 测试 `full_scan.py` 使用相同的文件夹结构（按时间戳组织报告）
- [ ] 验证 RECV 消息是否被正确记录（当前只有 SEND 被记录）
- [ ] 优化 `NetMessageMonitor` 的 `OnBeforeNotify` 回调（可能未正确挂钩）

### 11.2 中优先级
- [ ] 增加更多噪音过滤规则（根据实测补充）
- [ ] 优化 `click_test.py` 的按钮定位逻辑（当前使用 Poco 相对坐标）
- [ ] 增加 HTML 报告的协议消息可视化（表格展示）

### 11.3 低优先级
- [ ] 支持更多 Unity 版本（当前仅测试 2022.3.62f3）
- [ ] 增加自动化测试覆盖率报告
- [ ] 优化 `pywinauto` GameView 动态定位逻辑（当前可能找不到窗口）

---

## 附录 A：关键文件清单

| 文件 | 位置 | 说明 |
|---|---|---|
| `NetMessageMonitor.cs` | `Assets/Poco/` | 网络消息监控核心 C# 脚本 |
| `AutoStartPoco.cs` | `Assets/Poco/` | 自动初始化 Poco + NetMessagePoller |
| `net_monitor_watcher.py` | `AutoSmoke/modules/` | Python 端日志读取器 |
| `full_scan.py` | `AutoSmoke/` | 全 UI 扫描主脚本（灰盒模式） |
| `click_test.py` | `AutoSmoke/` | 底部导航栏点击验证测试 |
| `verify_protocol.py` | `AutoSmoke/` | 交互式协议验证工具 |
| `setup.py` | `AutoSmoke/` | 一键生成所有脚本（含正确路径） |
| `部署手册.md` | `AutoSmoke/` | 完整部署手册（含第 9 章自适应原理） |
| `templates/*.template` | `AutoSmoke/templates/` | C# / Bat 模板文件 |
| `net_messages.log` | `AutoSmoke/logs/` | 网络消息日志（C# 写入，Python 读取） |
| `redeploy_poco.bat` | `{{WORK_ROOT}}\` | 一键重新部署脚本 |
| 报告文件 | `AutoSmoke/reports/{timestamp}/` | 按时间戳组织的测试报告 |

---

## 附录 B：协议消息格式说明

`net_messages.log` 每行格式：

```
时间|方向|协议类型|内容摘要
```

示例：

```
19:25:01.123|SEND|cspb.ActvInfoReq|cspb.ActvInfoReq
19:25:01.456|RECV|cspb.ActvInfoAck|cspb.ActvInfoAck
19:25:02.789|RECV|cspb.ActvTmplQuestNtf|cspb.ActvTmplQuestNtf
```

| 字段 | 说明 |
|---|---|
| 时间 | `HH:mm:ss.fff` 格式 |
| 方向 | `SEND` = 客户端发送；`RECV` = 服务器下发 |
| 协议类型 | 完整 protobuf 类型名（如 `cspb.ActvInfoReq`） |
| 内容摘要 | protobuf 序列化后的文本表示（前 500 字符） |

---

## 9. 自动点击与分辨率自适应原理

> 本章节说明 Python 自动化脚本如何实现**不依赖固定坐标**的点击，以及在不同 Unity GameView 分辨率下的自适应原理。

### 9.1 问题背景

Unity Editor 的 GameView 窗口存在两个影响点击准确性的问题：

1. **黑边问题**：游戏渲染比例（如 1170×2532）和 GameView 窗口尺寸不一致时，左右会出现黑边，Poco 返回的是游戏内相对坐标（0~1），直接映射到窗口坐标会点偏。
2. **工具栏偏移**：Unity Editor 的 GameView 顶部有 ~40px 的工具栏（Gizmo、分辨率下拉等），Airtest 点击时需要跳过这部分。

### 9.2 GameView 窗口动态定位

脚本通过 `pywinauto` 自动查找 Unity Editor 的 GameView 子窗口，无需手动配置坐标。

**实现函数：`connect_and_calibrate()`**

```python
def connect_and_calibrate():
    # 1. 找到 Unity 主窗口（排除最小化到 -32000 的窗口）
    windows = pywinauto.findwindows.find_elements(class_name='UnityContainerWndClass')
    target = next(w for w in windows if w.rectangle.left > -1000)
    handle = target.handle

    # 2. 连接到 Windows 设备
    dev = Windows(handle=handle)
    app = dev._app

    # 3. 查找 GameView 子窗口
    game_view = app.top_window().child_window(title="UnityEditor.GameView")
    gv = game_view.wrapper_object()
    gv_rect = game_view.rectangle()

    # 4. 设置 Airtest  focus_rect，跳过顶部 40px 工具栏
    dev._top_window = gv
    dev.focus_rect = (0, 40, 0, 0)
    G.DEVICE = dev

    # 5. 获取游戏内分辨率（Poco 返回的游戏画布尺寸）
    poco = UnityPoco(('localhost', 5001), connect_default_device=False)
    sw, sh = poco.get_screen_size()   # 例如 (0.46, 1.0) 代表 1170x2532
```

**关键变量说明：**

| 变量 | 说明 |
|---|---|
| `gv_rect` | GameView 窗口的屏幕绝对坐标（left, top, right, bottom） |
| `handle` | Unity 窗口句柄，用于 Airtest 点击 |
| `focus_rect = (0, 40, 0, 0)` | 告诉 Airtest：顶部有 40px 工具栏，点击坐标 Y 要 +40 |

### 9.3 黑边偏移计算

当 GameView 窗口大小和游戏渲染比例不一致时，需要计算黑边宽度，将 Poco 相对坐标正确映射到渲染区域。

**计算公式：**

```python
# GameView 窗口尺寸
gv_w = gv_rect.right - gv_rect.left    # 例如 1280
gv_h = gv_rect.bottom - gv_rect.top    # 例如 720

# 减去顶部工具栏
toolbar = 40
work_w = gv_w                          # 可用宽度 = 窗口宽度
work_h = gv_h - toolbar                # 可用高度 = 窗口高度 - 40

# 根据游戏比例计算实际渲染宽度（保持比例）
# sw/sh 是游戏的宽高比，例如 1170/2532 ≈ 0.462
render_w = int(work_h * sw / sh)       # 按高度适配，计算宽度
bar_l = (work_w - render_w) // 2      # 左侧黑边宽度
```

**示例计算（游戏 1170×2532，GameView 1280×720）：**

```
sw, sh = 0.46, 1.0   （Poco 返回的归一化尺寸）
work_w = 1280
work_h = 720 - 40 = 680
render_w = int(680 × 0.46 / 1.0) = 312px
bar_l = (1280 - 312) / 2 = 484px
```

即：游戏画面在 GameView 中间 312px 宽的区域渲染，左右各有 484px 黑边。

**校准参数 `calib` 字典：**

```python
calib = {
    'work_w': work_w,     # GameView 可用宽度
    'work_h': work_h,     # GameView 可用高度（减工具栏）
    'render_w': render_w, # 游戏实际渲染宽度
    'bar_l': bar_l,       # 左侧黑边宽度
}
```

### 9.4 坐标转换公式

Poco 返回的节点坐标是**相对坐标**（0.0 ~ 1.0），需要转换为 GameView 的绝对像素坐标。

**转换公式：**

```python
# Poco 返回的相对坐标
px, py = elem.get_position()   # 例如 (0.5, 0.8)

# 转换为 GameView 绝对坐标
gv_x = px * calib['render_w'] + calib['bar_l']
gv_y = py * calib['work_h']

# Airtest 点击（focus_rect 已处理工具栏 40px 偏移，无需再加）
airtest_touch([int(gv_x), int(gv_y)])
```

**转换示例：**

```
假设点击"主城"按钮，Poco 返回坐标 (0.5, 0.92)

gv_x = 0.5 × 312 + 484 = 640
gv_y = 0.92 × 680 = 625.6

最终点击坐标：(640, 626)  ← 在 GameView 坐标系内
```

### 9.5 Y 坐标微调（y_offset）

部分按钮（如底部导航栏第 5 个按钮）下方有 Debug/GM 按钮，点击时容易误触。通过 `y_offset` 参数向上微调：

```python
# click_test.py 中的按钮配置
tests = [
    ('Train', '主城', 0.03),       # y_offset = 0.03（向上微调 3%）
    ('Skill', '小游戏', 0.03),
    ('Hero', '英雄', 0.03),
    ('Alliance', '联盟', 0.03),
    ('World', '世界地图', 0.03),    # 第5个按钮，容易误触下方GM按钮
]

# 应用偏移
px, py = elem.get_position()
py = max(0, py - y_off)   # 向上微调
```

### 9.6 不同分辨率下的行为

| GameView 尺寸 | 游戏比例 | 渲染宽度 | 黑边 | 点击是否准确 |
|---|---|---|---|---|
| 1280×720 | 1170×2532 (9:19.5) | 312px | 左右各 484px | ✅ 自动校准 |
| 1920×1080 | 1170×2532 (9:19.5) | 499px | 左右各 711px | ✅ 自动校准 |
|  Full HD | 1920×1080 (16:9) | 100% | 无黑边 | ✅ 无需校准 |
| 窗口模式 | 任意 | 动态计算 | 动态计算 | ✅ 每次运行重新校准 |

**核心优势**：每次运行脚本时都会重新调用 `connect_and_calibrate()` 计算校准参数，**无需手动修改任何坐标**。

### 9.7 关键代码位置

| 函数 | 文件 | 说明 |
|---|---|---|
| `connect_and_calibrate()` | `full_scan.py` / `click_test.py` | GameView 定位 + 校准参数计算 |
| `calibrated_click()` | `full_scan.py` | 坐标转换 + 点击执行 |
| `try_dismiss_popups()` | `full_scan.py` | 弹窗关闭（也使用校准坐标） |

### 9.8 故障排查

#### 点击位置偏了

1. **检查 `focus_rect` 是否正确设置**：
   ```python
   dev.focus_rect = (0, 40, 0, 0)   # 顶部 40px 工具栏
   ```

2. **检查 GameView 是否真的叫 `UnityEditor.GameView`**：
   - 在 Unity Editor 中，右键 Game 窗口标题栏 → 检查窗口标题
   - 某些 Unity 版本可能语言不同

3. **手动打印校准参数**：
   ```python
   print(f"GameView: {gv_w}x{gv_h}")
   print(f"渲染宽度: {render_w}, 黑边: {bar_l}px")
   ```

#### 点击到了黑边区域

通常是 `render_w` 计算错误，检查 `poco.get_screen_size()` 的返回值是否符合预期（应该是游戏的宽高比，不是窗口尺寸）。

---

## 附录 C：噪音过滤规则

以下消息会被 `NetMessageMonitor` 自动过滤，**不写入日志**：

| 协议类型 | 说明 |
|---|---|
| `HeartBeatReq` / `HeartBeatAck` | 心跳（每 10 秒） |
| `PositionNtf` | 位置同步 |
| `BIReport*` | BI 数据上报 |
| `RallyNtf` / `RallyList*` | 集结信息推送 |
| `GetUnionMessage*` | 联盟消息轮询 |

如需修改过滤规则，编辑 `NetMessageMonitor.cs` 中的 `IsNoiseType()` 方法。

---

## 附录 D：部署前检查清单

- [ ] 将全文中 `{{WORK_ROOT}}` 替换为实际工作根目录（如 `D:\AutoTest`）
- [ ] 将全文中 `{{UNITY_PROJECT}}` 替换为实际 Unity 工程路径
- [ ] `NetMessageMonitor.cs` 中的 `_logPath` 已修改
- [ ] `AutoStartPoco.cs` 中 `NetMessageMonitor.Init()` 的路径已修改
- [ ] `redeploy_poco.bat` 中的路径已修改
- [ ] Unity 工程能正常编译，Console 显示 `[NetMonitor] Initialized`
- [ ] `net_messages.log` 文件能正常写入
- [ ] Python 端能正常读取日志

---

*文档结束*
