# AutoSmoke IDE 未实现功能清单

更新日期：2026-06-16  
依据：设计方案 + 当前 `debug_panel.py` 与记忆记录比对

## P0（先做）

- [ ] `GET /api/mapping/unmapped` 接口未接通真实数据  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py`  
  当前行为：固定返回 `{"unmapped": []}`。  
  结果影响：映射缺失列表页签无法展示真实数据，阻塞映射完整性检查。

- [ ] 环境初始化按钮功能未完成真实流程（Unity 启动、登录、日志抓取、基础检测）  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py`（`envInit()`）  
  当前行为：仅前端文本倒计时/提示，未见实际后台触发或状态回填。  
  结果影响：当前环境准备链路不可信，后续用例、截图、自动化流程依赖该能力。

- [ ] 迁移检查面板仍存在占位/硬编码项（Bridge、截图、点击等）  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py`（`ldMigrate()`）  
  当前行为：部分检查项固定显示“需检查”，缺少真实探测调用。  
  结果影响：迁移可用性评估结果不可靠。

## P1（高优先）

- [ ] `stateDiff` 面板内容仍为占位文案“需对接业务状态接口”  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py`（前端脚本/页面渲染）  
  结果影响：页面状态差异无法用于定位实际回归或异常。

- [ ] `pgR` 页面关系图数据展示占位（“需运行自动探索后生成”）  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py`（`pgR` 面板）  
  结果影响：缺少页面关系图对页面路径和层级关系的可视化支持。

- [ ] 迁移/映射相关操作在多个页签未形成统一状态链路（未统一刷新、未统一错误提示）  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py`  
  结果影响：同一对象在不同模块间状态不同步，用户体验和故障排查效率低。

## P2（中优先）

- [ ] 设计方案中的“页面关系图/异常链路/状态导出”未全部落到 UI 与接口  
  文件/位置：`E:/zdcs/IDE界面设计/AutoSmoke_IDE三大页签界面布局设计方案.md` 与 `E:/zdcs/AutoSmoke/IDE/debug_panel.py`  
  结果影响：功能边界和产出物与方案不一致，后续文档与实现回归困难。

- [ ] 与外部任务系统/服务端日志平台的联动核验缺失（若方案中定义）  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py`  
  结果影响：无法从端侧直接回放和追踪全链路执行证据。

- [ ] 多处“占位文案”缺乏状态来源和失败重试策略  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py`  
  结果影响：页面看似有功能入口但不可复现或不可恢复。

## P3（持续优化）

- [ ] 接口返回错误场景下的幂等重试与降级策略补齐  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py` 与后端相关路由  
  结果影响：网络抖动或环境波动时体验不稳定。

- [ ] 操作记录、审计日志与导出格式统一  
  文件/位置：`E:/zdcs/AutoSmoke/IDE/debug_panel.py`  
  结果影响：后续问题复现与对账耗时增加。

- [ ] 配置与测试用例的“页面级映射一致性”自动校验机制  
  文件/位置：后端映射、测试生成与前端映射页签  
  结果影响：上线回归覆盖不足。

## 备注

- 本清单按“阻塞/功能可用性”为核心分级，建议优先执行 P0 → P1 → P2。  
- 建议每完成一项在记忆文件中补一条执行记录（含时间、修改文件、影响范围、验证结果）。
