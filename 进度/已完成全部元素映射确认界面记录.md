# 已完成全部元素映射确认界面记录

本文件用于记录 AutoSmoke 当前已经完成“全部元素映射确认”的界面，作为产品化进度台账。

## 记录规则

- 只有当某个界面的目标生成、候选匹配、视觉确认、点击确认、正式映射写入均已闭环后，才记录为“已完成”。
- 记录前应完成语法检查、索引重建、legacy 导出和 `/api/mapping_store/validate` 校验。
- 如果后续发现该界面存在 identity issue、通用兜底 semanticId、路径误合并或目标名被刷新污染，应将状态改为“需复核”。
- 记录时优先填写业务界面名和稳定 `pageId`，避免只写 runtime 名称。

## 已完成界面

| 序号 | 界面名称 | pageId | 确认范围 | 校验状态 | 完成日期 | 备注 |
|------|----------|--------|----------|----------|----------|------|
| 4 | 背包界面 | bag | formal 16 项；目标 draft 12 项；点击 15 项；视觉 1 项 | 已完成；validate passed；identityConflictCount=0 | 2026-06-25 | 自动检测通过后写入 |

## 状态说明

| 状态 | 含义 |
|------|------|
| 已完成 | 全部元素映射已确认，并通过当前校验 |
| 需复核 | 曾完成，但后续发现数据或规则需要重新确认 |
| 已回归 | 复核后重新通过校验 |

## 最近校验记录

| 日期 | 校验项 | 结果 | 备注 |
|------|--------|------|------|
| 2026-06-25 | 页面完成检测 | 通过 | pageId=bag; formal=16; identityConflictCount=0; absolutePathHitCount=0 |
